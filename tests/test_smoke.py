"""Placeholder smoke test.

Replace with real unit tests for `arch_common` once that shared package is extracted
(around T3-T4, per the plan). Exists now only so CI's pytest step has something to run.
"""


def test_scaffold_sane() -> None:
    assert True
